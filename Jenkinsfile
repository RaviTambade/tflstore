pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Example: Run a shell command
                sh 'echo "Building..."'
            }
        }
        stage('Test') {
            steps {
                // Example: Run unit tests
                sh 'echo "Testing..."'
            }
        }
        stage('Deploy') {
            steps {
                // Example: Deploy to a server
                sh 'echo "Deploying..."'
            }
        }
    }
}
